package lock;

/**
 * Implements the Lock object which is used to carry out "synchronized" operations including wait() and notify(). This allows a transparent mechanism for putting
 * threads on hold and waking them up from said holds.
 */

public class Lock
{

}
